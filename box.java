void main() {
    IO.print("Enter number of box : ");
    int box = Integer.parseInt(IO.readln());
    IO.print("Enter of your pencil : ");
    int pencil = Integer.parseInt(IO.readln());
    int anwser = box * pencil;
    IO.println("Total number of pencils = " + anwser);
}